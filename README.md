# Fetch remote Excel files then serve to the client

A simple project which fetches an Excel file from a URL, downloads it, and then serves it to the browser.

It uses the following libraries:

- Express
- Request
- ExcelJs
